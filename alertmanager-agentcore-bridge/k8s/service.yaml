apiVersion: v1
kind: Service
metadata:
  name: alertmanager-agentcore-bridge
  namespace: alertmanager-agentcore-bridge
  labels:
    app.kubernetes.io/name: alertmanager-agentcore-bridge
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: alertmanager-agentcore-bridge
